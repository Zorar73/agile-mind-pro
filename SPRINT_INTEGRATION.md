# Интеграция Sprint системы

## Что сделано ✅

### 1. Добавлен маршрут в App.jsx

**Файл:** `src/App.jsx`

**Изменения:**
- Добавлен import: `const SprintPlanning = lazy(() => import('./components/Sprint/SprintPlanning'));`
- Добавлен route: `/board/:boardId/sprints`

```javascript
{/* Спринты доски */}
<Route path="/board/:boardId/sprints" element={<ProtectedRoute><SprintPlanning /></ProtectedRoute>} />
```

### 2. Добавлена кнопка на BoardPage

**Файл:** `src/pages/BoardPage.jsx`

**Изменения:**
- Добавлен import иконки: `TrendingUp`
- Добавлена кнопка в header доски (после кнопки "Спринт")

```javascript
{/* Все спринты */}
<Tooltip title="Управление спринтами">
  <IconButton
    size="small"
    onClick={() => navigate(`/board/${boardId}/sprints`)}
    sx={{ color: 'primary.main' }}
  >
    <TrendingUp />
  </IconButton>
</Tooltip>
```

## Доступные компоненты Sprint системы

Все компоненты уже созданы и готовы к использованию:

### 1. SprintPlanning.jsx
- Главная страница управления спринтами
- Планирование нового спринта
- Добавление задач в спринт
- Установка целей и дедлайнов

### 2. ActiveSprint.jsx
- Отображение активного спринта
- Прогресс выполнения
- Список задач в спринте
- Действия со спринтом (завершить, отменить)

### 3. BurndownChart.jsx
- График выгорания спринта
- Визуализация прогресса
- Идеальная линия vs фактический прогресс
- Прогноз завершения

### 4. SprintHistory.jsx
- История завершенных спринтов
- Статистика по каждому спринту
- Метрики (velocity, completion rate)

### 5. SprintStats.jsx
- Детальная статистика спринта
- KPI и метрики
- Сравнение с предыдущими спринтами

## Как использовать

### Доступ к Sprint системе

1. **Через кнопку на доске:**
   - Открой любую доску
   - В header доски найди иконку 📈 (TrendingUp)
   - Клик → откроется страница управления спринтами

2. **Прямой URL:**
   - `/board/{boardId}/sprints`
   - Например: `/board/abc123/sprints`

### Создание спринта

1. Перейди на страницу спринтов
2. Нажми "Создать спринт"
3. Заполни:
   - Название спринта
   - Цель спринта
   - Даты начала и окончания
4. Добавь задачи из бэклога
5. Начни спринт

### Работа со спринтом

1. **Просмотр прогресса:**
   - График выгорания (Burndown Chart)
   - Статистика по задачам
   - Оставшееся время

2. **Управление задачами:**
   - Добавить задачу в спринт
   - Удалить задачу из спринта
   - Изменить статус задачи

3. **Завершение спринта:**
   - Кнопка "Завершить спринт"
   - Автоматический расчет метрик
   - Перенос незавершенных задач

## Технические детали

### Сервис: sprint.service.js

**Основные методы:**

```javascript
// Создать спринт
sprintService.createSprint(boardId, sprintData)

// Получить активный спринт доски
sprintService.getActiveSprint(boardId)

// Получить все спринты доски
sprintService.getBoardSprints(boardId)

// Начать спринт
sprintService.startSprint(sprintId)

// Завершить спринт
sprintService.completeSprint(sprintId)

// Добавить задачи в спринт
sprintService.addTasksToSprint(sprintId, taskIds)

// Получить статистику спринта
sprintService.getSprintStats(sprintId)
```

### Структура данных Sprint

```javascript
{
  id: 'sprint-id',
  boardId: 'board-id',
  name: 'Sprint 1',
  goal: 'Implement user authentication',
  startDate: Timestamp,
  endDate: Timestamp,
  status: 'planned' | 'active' | 'completed',
  taskIds: ['task-1', 'task-2', ...],
  createdBy: 'user-id',
  createdAt: Timestamp,
  completedAt: Timestamp | null,
  metrics: {
    totalTasks: 10,
    completedTasks: 7,
    velocity: 21,
    completionRate: 70
  }
}
```

### Firestore правила

Убедись, что правила для коллекции `sprints` применены (уже есть в `firestore.rules.new`):

```javascript
match /sprints/{sprintId} {
  allow list, get: if isSignedIn();
  allow create: if isApproved();
  allow update: if isSignedIn();
  allow delete: if isSignedIn() && resource.data.createdBy == request.auth.uid;
}
```

## Возможности Sprint системы

### ✅ Уже реализовано:

- Создание и планирование спринтов
- Добавление задач в спринт
- Старт и завершение спринтов
- Burndown chart (график выгорания)
- История спринтов
- Статистика и метрики
- Velocity tracking
- Автоматический расчет прогресса

### 🎯 Возможные улучшения (опционально):

- Recurring sprints (автоматическое создание)
- Sprint templates (шаблоны)
- Team capacity planning (планирование мощности команды)
- Retrospective notes (ретроспектива)
- Sprint comparison (сравнение спринтов)
- Export sprint reports (экспорт отчетов)

## Тестирование

### Шаг 1: Открыть страницу спринтов

1. Открой доску
2. Найди иконку 📈 в header
3. Клик → должна открыться страница SprintPlanning

### Шаг 2: Создать спринт

1. Нажми "Создать спринт"
2. Заполни форму:
   - Название: "Тестовый спринт"
   - Цель: "Проверка функционала"
   - Даты: на 2 недели вперед
3. Сохрани

### Шаг 3: Добавить задачи

1. Выбери задачи из бэклога
2. Нажми "Добавить в спринт"
3. Задачи должны появиться в списке спринта

### Шаг 4: Начать спринт

1. Нажми "Начать спринт"
2. Статус должен измениться на "Active"
3. Появится Burndown Chart

### Шаг 5: Проверить прогресс

1. Измени статус задач на доске
2. Вернись на страницу спринта
3. График должен обновиться
4. Статистика должна измениться

### Шаг 6: Завершить спринт

1. Нажми "Завершить спринт"
2. Должен появиться отчет с метриками
3. Незавершенные задачи предложит перенести

## Интеграция с другими модулями

### С Board (Доска)

- Задачи из доски можно добавлять в спринт
- Изменение статуса на доске обновляет прогресс спринта
- Фильтр "Только спринт" показывает задачи текущего спринта

### С Backlog (Бэклог)

- Задачи из бэклога можно добавлять в планируемый спринт
- Приоритизация задач в бэклоге помогает планировать спринт

### С Calendar (Календарь)

- Даты спринта отображаются в календаре
- Дедлайны задач спринта видны в календаре

### С Dashboard (Дашборд)

- Виджет активного спринта на главной
- Прогресс спринта в статистике
- Velocity chart (график скорости команды)

## Troubleshooting

### Страница спринтов не открывается

1. Проверь консоль браузера (F12) на ошибки
2. Убедись, что файл существует: `src/components/Sprint/SprintPlanning.jsx`
3. Проверь, что route добавлен в App.jsx

### Кнопка не видна на доске

1. Очисти кэш браузера (Ctrl+Shift+R)
2. Проверь, что иконка TrendingUp импортирована
3. Проверь, что navigate определен в компоненте

### Ошибка при создании спринта

1. Проверь Firestore Rules (должны быть применены)
2. Проверь права доступа пользователя
3. Проверь формат данных спринта

### График Burndown не обновляется

1. Проверь, что задачи действительно в спринте
2. Убедись, что статусы задач меняются
3. Перезагрузи страницу (может быть кэш)

## Итого

Sprint система полностью интегрирована и готова к использованию! 🎉

**Добавлено:**
- ✅ Маршрут `/board/:boardId/sprints`
- ✅ Кнопка на странице доски (иконка 📈)
- ✅ Все компоненты подключены
- ✅ Сервис работает
- ✅ Firestore Rules готовы

**Можно использовать прямо сейчас!**

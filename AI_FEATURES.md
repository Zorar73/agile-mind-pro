# AI Features Documentation

## Обзор AI-функционала

Agile Mind Pro интегрирован с Google Gemini AI и Puter.js для предоставления расширенных AI-возможностей.

---

## 🤖 Настройка AI

### Переменные окружения

```env
VITE_GEMINI_API_KEY=your_gemini_api_key_here
```

### Конфигурация

Файл: `src/config/ai.js`

- **Текстовый AI**: Google Gemini (gemini-2.5-flash)
- **Генерация изображений**: Puter.js (бесплатно, без API ключей)

---

## 📋 AI Сервисы для Задач

### 1. Разбиение задачи на подзадачи

**Метод**: `aiService.breakdownTask(task)`

**Описание**: Автоматически разбивает сложную задачу на список подзадач с оценкой времени.

**Пример использования**:
```javascript
import aiService from './services/ai.service';

const result = await aiService.breakdownTask({
  title: 'Создать страницу регистрации',
  description: 'Пользователь должен иметь возможность зарегистрироваться'
});

// Результат:
{
  success: true,
  subtasks: [
    { title: 'Создать форму регистрации', estimatedHours: 2 },
    { title: 'Добавить валидацию полей', estimatedHours: 1 },
    { title: 'Интегрировать с API', estimatedHours: 3 }
  ]
}
```

**Компонент**: `TaskAIAssistant`

---

### 2. Предложение тегов

**Метод**: `aiService.suggestTags(title, description, existingTags)`

**Описание**: Предлагает релевантные теги на основе названия и описания задачи.

**Пример**:
```javascript
const result = await aiService.suggestTags(
  'Исправить баг с авторизацией',
  'Пользователи не могут войти через Google',
  ['bug', 'urgent', 'frontend', 'backend']
);

// Результат:
{
  success: true,
  tags: ['bug', 'auth', 'oauth', 'urgent']
}
```

---

### 3. Оценка времени выполнения

**Метод**: `aiService.estimateTaskTime(task)`

**Описание**: Оценивает время выполнения задачи и дает уровень уверенности.

**Пример**:
```javascript
const result = await aiService.estimateTaskTime({
  title: 'Оптимизировать запросы к БД',
  description: 'Добавить индексы и кэширование'
});

// Результат:
{
  success: true,
  estimatedHours: 8,
  confidence: 'medium',
  reasoning: 'Задача требует анализа запросов и тестирования'
}
```

---

### 4. Генерация описания задачи

**Компонент**: `TaskDescriptionGenerator`

**Описание**: Автоматически создает подробное структурированное описание на основе названия задачи.

**Использование**:
```jsx
<TaskDescriptionGenerator
  title={task.title}
  currentDescription={task.description}
  onApply={(generatedDescription) => {
    setTaskData({ ...taskData, description: generatedDescription });
  }}
/>
```

**Формат генерации**:
- Цель задачи
- Основные шаги выполнения
- Критерии приемки
- Markdown форматирование

---

### 5. Извлечение задач из текста

**Метод**: `aiService.analyzeRecap(recapText, context)`

**Описание**: Извлекает actionable задачи из протоколов встреч или заметок.

**Пример**:
```javascript
const text = `
Встреча по планированию спринта:
- Иван займется рефакторингом компонентов
- Необходимо исправить баг с уведомлениями до пятницы
- Мария подготовит дизайн новой формы
`;

const result = await aiService.analyzeRecap(text, {
  boards: [{ id: 'board1', title: 'Sprint 12' }],
  tags: ['frontend', 'backend', 'design'],
  users: [{ firstName: 'Иван' }, { firstName: 'Мария' }]
});

// Результат:
{
  success: true,
  tasks: [
    {
      title: 'Рефакторинг компонентов',
      suggestedAssignee: '��ван',
      suggestedPriority: 'normal'
    },
    {
      title: 'Исправить баг с уведомлениями',
      suggestedDueDate: 'пятница',
      suggestedPriority: 'urgent'
    }
  ]
}
```

---

### 6. Преобразование скетчей в задачи

**Метод**: `aiService.sketchToTasks(sketchTitle, sketchContent)`

**Описание**: Конвертирует заметки и идеи в структурированные задачи.

**Пример**:
```javascript
const result = await aiService.sketchToTasks(
  'Идеи для новых фич',
  'Добавить темную тему, улучшить поиск, интегрировать календарь'
);

// Результат:
{
  success: true,
  tasks: [
    { title: 'Добавить темную тему', priority: 'normal' },
    { title: 'Улучшить поиск', priority: 'normal' },
    { title: 'Интегрировать календарь', priority: 'normal' }
  ]
}
```

---

### 7. Умный поиск задач

**Метод**: `aiService.smartSearch(query, tasks)`

**Компонент**: `SmartSearch`

**Описание**: Находит задачи используя естественный язык.

**Пример**:
```javascript
const result = await aiService.smartSearch(
  'все срочные задачи по фронтенду от Ивана',
  allTasks
);

// Результат:
{
  success: true,
  taskIds: ['task1', 'task2', 'task3']
}
```

**Использование компонента**:
```jsx
<SmartSearch
  tasks={tasks}
  onSelectTasks={(foundTasks) => {
    highlightTasks(foundTasks);
  }}
/>
```

---

### 8. Анализ производительности команды

**Метод**: `aiService.analyzeTeamPerformance(teamStats)`

**Описание**: Анализирует метрики команды и дает рекомендации.

**Пример**:
```javascript
const result = await aiService.analyzeTeamPerformance({
  totalTasks: 50,
  completedTasks: 35,
  inProgressTasks: 10,
  overdueTasks: 5,
  membersCount: 5
});

// Результат:
{
  success: true,
  summary: 'Команда показывает хорошие результаты',
  healthScore: 75,
  recommendations: [
    'Уменьшить количество просроченных задач',
    'Оптимизировать распределение нагрузки'
  ],
  risks: ['5 задач просрочено']
}
```

---

### 9. Резюмирование и расширение скетчей

**Методы**:
- `aiService.summarizeSketch(content)` - краткое резюме (2-3 предложения)
- `aiService.expandSketch(content)` - детализация идеи

**Пример**:
```javascript
// Резюмирование
const summary = await aiService.summarizeSketch(longContent);
// Результат: короткое описание

// Расширение
const expanded = await aiService.expandSketch(shortIdea);
// Результат: детальное описание с примерами
```

---

## 🎨 Генерация изображений

### Puter.js (Бесплатно)

**Метод**: `aiService.generateImage(prompt, options)`

**Доступные модели**:
- `flux-schnell` (по умолчанию) - быстро, хорошее качество
- `dall-e-3` - высокое качество
- `stable-diffusion-xl` - детальные изображения

**Пример**:
```javascript
const result = await aiService.generateImage(
  'Modern office workspace, minimalist design',
  { model: 'flux-schnell' }
);

// Результат:
{
  success: true,
  imageUrl: 'data:image/png;base64,...',
  provider: 'puter',
  model: 'flux-schnell'
}
```

### Генерация аватаров

**Метод**: `aiService.generateAvatar(description, options)`

**Пример**:
```javascript
const result = await aiService.generateAvatar(
  'Young professional developer, friendly smile',
  { width: 512, height: 512 }
);
```

---

## 🔧 Компоненты

### TaskAIAssistant

AI-помощник для задач с кнопками:
- Разбить на подзадачи
- Предложить теги
- Оценить время

**Использование**:
```jsx
import TaskAIAssistant from './components/Task/TaskAIAssistant';

<TaskAIAssistant
  task={currentTask}
  onAddSubtask={(subtask) => createSubtask(subtask)}
  onAddTags={(tags) => addTagsToTask(tags)}
  onUpdateTime={(hours) => setEstimatedHours(hours)}
  existingTags={['frontend', 'backend']}
/>
```

### TaskDescriptionGenerator

Генерация описаний задач.

**Использование**:
```jsx
import TaskDescriptionGenerator from './components/Task/TaskDescriptionGenerator';

<TaskDescriptionGenerator
  title={task.title}
  currentDescription={task.description}
  onApply={(description) => updateTaskDescription(description)}
/>
```

### SmartSearch

Умный поиск с AI.

**Использование**:
```jsx
import SmartSearch from './components/Board/SmartSearch';

<SmartSearch
  tasks={allTasks}
  onSelectTasks={(tasks) => setFilteredTasks(tasks)}
/>
```

---

## 📊 Ограничения и квоты

### Google Gemini (Бесплатный tier)
- **Запросы**: 15 запросов в минуту
- **Токены**: 1 миллион в день
- **Модель**: gemini-2.5-flash

### Puter.js
- **Полностью бесплатно**
- Без API ключей
- Без лимитов

---

## 🔐 Безопасность

- API ключи хранятся в `.env` (не коммитятся в Git)
- Все запросы выполняются с клиента
- Нет отправки чувствительных данных
- Используются только публичные модели

---

## 🚀 Расширение функционала

### Добавление нового AI-метода

1. Добавить метод в `src/services/ai.service.js`:
```javascript
async myNewAIFeature(input) {
  if (!this.isInitialized()) {
    return { success: false, error: 'AI не инициализирован' };
  }

  try {
    const prompt = `Ваш промпт: ${input}`;
    const result = await this.model.generateContent(prompt);
    const text = (await result.response).text().trim();
    return { success: true, result: text };
  } catch (error) {
    return { success: false, error: error.message };
  }
}
```

2. Использовать в компоненте:
```javascript
const result = await aiService.myNewAIFeature('test input');
```

---

## 📝 Примеры промптов

### Хорошие промпты для разбиения задач:
✅ "Создать систему аутентификации пользователей"
✅ "Оптимизировать производительность дашборда"
✅ "Реализовать экспорт данных в Excel"

### Плохие промпты:
❌ "Сделать что-то" (слишком общее)
❌ "Исправить" (нет контекста)

---

## 🎯 Best Practices

1. **Используйте AI для рутинных задач**: разбиение, оценка времени, теги
2. **Проверяйте результаты**: AI может ошибаться
3. **Давайте контекст**: чем больше информации, тем лучше результат
4. **Экспериментируйте**: пробуйте разные формулировки
5. **Не отправляйте секретные данные**: API ключи, пароли, личные данные

---

## 🐛 Troubleshooting

### AI не работает
1. Проверьте `VITE_GEMINI_API_KEY` в `.env`
2. Убедитесь что ключ валиден
3. Проверьте консоль на ошибки
4. Проверьте квоты Gemini API

### Изображения не генерируются
1. Puter.js должен загрузиться (проверьте консоль)
2. Попробуйте другую модель
3. Обновите страницу

### Медленная работа
1. Gemini может быть перегружен
2. Проверьте интернет соединение
3. Используйте более короткие промпты

---

## 📚 Дополнительные ресурсы

- [Google Gemini API Docs](https://ai.google.dev/docs)
- [Puter.js Documentation](https://docs.puter.com)
- [React Window Docs](https://react-window.vercel.app)

---

Создано для **Agile Mind Pro** | Последнее обновление: 2025-12-16

# Исправление функционала вложений файлов

## Проблема

Пользователи могли загружать файлы, но НЕ могли:
- ✗ Просматривать загруженные файлы после перезагрузки
- ✗ Воспроизводить видео/аудио
- ✗ Скачивать файлы
- ✗ Открывать PDF и другие документы

## Причина

### 1. Файлы НЕ загружались в Cloudinary

В `CommentInput.jsx` (строки 83-93):
```javascript
const handleFileAttach = (e) => {
  const file = e.target.files[0];
  // TODO: Загрузка файла через storage service  ← Не реализовано!
  setAttachments([...attachments, {
    url: URL.createObjectURL(file),  ← Локальный blob URL!
  }]);
};
```

**Проблема:** Файл не загружался в облако, вместо этого создавался временный `blob:` URL:
- Работает только в текущей сессии браузера
- Не сохраняется в базе данных
- Исчезает после перезагрузки страницы

### 2. Вложения отображались как текст

В `ThreadedComment.jsx` (строки 252-268):
```javascript
{att.type === 'image' ? (
  <img src={att.url} alt={att.name} />
) : (
  <Typography>📎 {att.name}</Typography>  ← Только текст!
)}
```

**Проблема:** Для файлов (не картинок) показывался только текст - БЕЗ:
- Ссылок для скачивания
- Плееров для видео/аудио
- Возможности открыть файл

## Что исправлено

### ✅ 1. Загрузка файлов в Cloudinary

**Файл:** `src/components/Common/CommentInput.jsx`

**Изменения:**
- Добавлен `import cloudinaryService`
- Функция `handleFileAttach` теперь **асинхронная**
- Файл загружается через `cloudinaryService.upload(file, { folder: 'attachments' })`
- Определяется тип файла (image/video/audio/pdf/file)
- Сохраняется полная информация:
  ```javascript
  {
    type: 'video',       // image/video/audio/pdf/file
    name: 'video.mp4',
    url: 'https://res.cloudinary.com/...',  // ← Постоянный CDN URL!
    mimeType: 'video/mp4',
    size: 5242880
  }
  ```
- Добавлен индикатор загрузки (спиннер)
- Проверка размера файла (макс 10MB)

### ✅ 2. Компонент просмотра вложений

**Файл:** `src/components/Common/AttachmentViewer.jsx` (создан)

**Функционал:**
- **Изображения** → Превью с возможностью открыть в полном размере
- **Видео** → Встроенный `<video>` плеер с controls
- **Аудио** → Встроенный `<audio>` плеер с controls
- **PDF** → Кнопки "Открыть" и "Скачать"
- **Другие файлы** → Кнопки "Открыть" и "Скачать"

**Пример:**
```jsx
<AttachmentViewer
  attachment={{
    type: 'video',
    name: 'demo.mp4',
    url: 'https://...',
    mimeType: 'video/mp4'
  }}
  size="medium"
/>
```

### ✅ 3. Интеграция в ThreadedComment

**Файл:** `src/components/Common/ThreadedComment.jsx`

Старое:
```javascript
{att.type === 'image' ? <img src={att.url} /> : <Typography>📎 {att.name}</Typography>}
```

Новое:
```javascript
<AttachmentViewer attachment={att} size="medium" />
```

### ✅ 4. Улучшенное превью в CommentInput

**Файл:** `src/components/Common/CommentInput.jsx`

Теперь показывает:
- Правильные иконки по типу файла (🖼️ 🎥 🎵 📄)
- Размер файла в читаемом формате
- Кнопку удаления из превью

## Поддерживаемые типы файлов

| Тип | Отображение | Действия |
|-----|-------------|----------|
| **Изображения** (jpg, png, gif, webp) | Превью с возможностью открыть | Клик → открыть в новой вкладке |
| **Видео** (mp4, webm, mov) | Встроенный плеер | Play/Pause, громкость, полноэкранный режим |
| **Аудио** (mp3, wav, ogg) | Встроенный плеер | Play/Pause, перемотка, громкость |
| **PDF** | Иконка и название | "Открыть" / "Скачать" |
| **Документы** (doc, xls, txt) | Иконка и название | "Открыть" / "Скачать" |
| **Остальное** | Иконка и название | "Открыть" / "Скачать" |

## Как протестировать

### 1. Загрузка файлов

1. Открой любую страницу с комментариями (новости, задачи, скетчи)
2. Нажми на иконку 📎 "Прикрепить файл"
3. Выбери файл (до 10MB)
4. Должен появиться спиннер загрузки
5. После загрузки - превью с иконкой и размером
6. Отправь комментарий

### 2. Просмотр изображений

1. Прикрепи изображение (jpg/png)
2. В комментарии должно показаться превью
3. Клик по превью → открывается в новой вкладке в полном размере

### 3. Воспроизведение видео

1. Прикрепи видео (mp4)
2. В комментарии должен появиться видео-плеер
3. Нажми ▶️ Play - видео должно воспроизводиться
4. Проверь громкость и полноэкранный режим

### 4. Воспроизведение аудио

1. Прикрепи аудио (mp3)
2. В комментарии появится аудио-плеер
3. Нажми ▶️ Play - аудио должно воспроизводиться
4. Проверь перемотку и громкость

### 5. Скачивание файлов

1. Прикрепи PDF или документ
2. В комментарии появится карточка с кнопками
3. Нажми "Скачать" - файл должен скачаться
4. Нажми "Открыть" - файл откроется в новой вкладке

### 6. Перезагрузка страницы

1. Прикрепи файл к комментарию
2. Отправь комментарий
3. **Перезагрузи страницу (Ctrl+Shift+R)**
4. Файл должен ОСТАТЬСЯ доступным
5. Видео/аудио должны воспроизводиться
6. Кнопки скачивания должны работать

## Технические детали

### Структура attachment объекта

```javascript
{
  type: 'image' | 'video' | 'audio' | 'pdf' | 'file',
  name: 'filename.ext',
  url: 'https://res.cloudinary.com/...',  // Постоянный CDN URL
  mimeType: 'video/mp4',                   // MIME-тип
  size: 5242880                            // Размер в байтах
}
```

### Где сохраняются файлы

- **Cloudinary папка:** `agile-mind-pro/attachments/`
- **Теги:** `attachments, image` (автоматически)
- **Формат:** Оригинальный (без конвертации)

### Ограничения

- Максимальный размер файла: **10 MB**
- Поддержка всех MIME-типов
- Неограниченное хранилище (в рамках Cloudinary плана)

## Если что-то не работает

### Файл не загружается

1. **Проверь размер файла** - максимум 10MB
2. **Проверь Cloudinary конфиг:**
   - Открой `/cloudinary-test`
   - Убедись что cloudName и uploadPreset правильные
3. **Проверь консоль браузера (F12)**
   - Должно быть сообщение об ошибке

### Файл загрузился, но не показывается

1. **Перезагрузи страницу (Ctrl+Shift+R)**
2. **Проверь консоль** - нет ли ошибок
3. **Проверь URL файла:**
   ```javascript
   // В консоли браузера
   console.log(comment.attachments)
   ```
   URL должен начинаться с `https://res.cloudinary.com/`

### Видео/аудио не воспроизводится

1. **Проверь формат файла** - браузер может не поддерживать
   - Видео: mp4, webm (рекомендуется)
   - Аудио: mp3, ogg (рекомендуется)
2. **Попробуй открыть URL напрямую** - если не работает, проблема в файле
3. **Проверь CORS** - Cloudinary должен отдавать правильные заголовки

## Преимущества

- ✅ Файлы хранятся постоянно в облаке
- ✅ Быстрая загрузка через CDN
- ✅ Автоматическая оптимизация
- ✅ Встроенный просмотр для всех типов
- ✅ Одна строка кода для отображения любого файла
- ✅ Отзывчивый дизайн (адаптивные размеры)

## Что дальше (опционально)

Можно добавить:
- 🎨 Редактор изображений перед загрузкой
- 📊 Статистика по загрузкам
- 🔐 Приватные файлы (только для участников доски)
- 📦 Групповая загрузка (drag & drop нескольких файлов)
- 🗜️ Автоматическое сжатие больших файлов

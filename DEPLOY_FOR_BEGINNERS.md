# 🚀 ДЕПЛОЙ ДЛЯ НОВИЧКОВ: Пошаговая инструкция

**Для тех, кто делает это впервые!**

Эта инструкция написана максимально подробно. Следуй каждому шагу по порядку.

---

## 📋 ЧТО ТЕБЕ ПОНАДОБИТСЯ

1. **Твой проект** - папка `frontend` на компьютере
2. **Timeweb Cloud аккаунт** - зарегистрируйся на https://timeweb.cloud
3. **Домен с Reg.ru** (или любой другой регистратор)
4. **Деньги на балансе** Timeweb (~300₽/месяц за сервер)
5. **Программа для SSH** (уже есть в Windows 10+)
6. **30-60 минут времени**

---

## ЭТАП 1: ПОДГОТОВКА ЛОКАЛЬНО (15 минут)

### Шаг 1.1: Проверь Firebase Rules ⚠️ КРИТИЧНО!

**Зачем:** Без этого приложение будет небезопасно!

**Что делать:**

1. Открой браузер
2. Перейди на https://console.firebase.google.com
3. Войди в аккаунт Google
4. Найди проект **agile-mind-pro** (кликни на него)
5. В левом меню найди **Firestore Database** → кликни
6. Сверху найди вкладку **Правила** (или **Rules**) → кликни
7. Ты увидишь окно с кодом

**Теперь:**

8. Открой на компьютере файл `frontend/firestore.rules.new`
9. Скопируй ВСЁ содержимое этого файла (Ctrl+A, Ctrl+C)
10. Вернись в браузер с Firebase Console
11. Выдели весь текст в окне правил (Ctrl+A)
12. Вставь скопированное (Ctrl+V)
13. Нажми кнопку **Опубликовать** (или **Publish**) справа сверху
14. Подожди 5-10 секунд - появится зелёная галочка "Правила обновлены"

✅ **Готово!** Можно закрыть вкладку Firebase.

---

### Шаг 1.2: Собери production версию

**Зачем:** Нужно создать оптимизированные файлы для сервера.

**Что делать:**

1. Открой **PowerShell** или **Командную строку**
   - **Windows:** Нажми Win+R, введи `powershell`, Enter

2. Перейди в папку проекта:
   ```powershell
   cd C:\Users\1\agile-mind-pro\frontend
   ```
   (Если твоя папка в другом месте - замени путь!)

3. Запусти сборку:
   ```powershell
   npm run build
   ```

4. **Подожди 5-10 секунд**. Увидишь много текста.

5. **Проверь результат:**
   - В конце должно быть: `✓ built in 5.45s` (зелёная галочка)
   - НЕ должно быть красных ошибок
   - Появится папка `dist` в `frontend/`

✅ **Готово!** Файлы для деплоя готовы в папке `dist/`.

---

### Шаг 1.3: Протестируй локально (опционально, но РЕКОМЕНДУЮ)

**Зачем:** Проверить, что всё работает ПЕРЕД деплоем.

**Что делать:**

1. В той же PowerShell введи:
   ```powershell
   npm run preview
   ```

2. Откроется браузер с адресом http://localhost:4173

3. **Проверь основные функции:**
   - [ ] Логин работает
   - [ ] Доски открываются
   - [ ] Задачи создаются
   - [ ] Нет красных ошибок в консоли (F12)

4. Закрой preview (Ctrl+C в PowerShell)

✅ **Готово!** Локальный билд работает, можно деплоить.

---

## ЭТАП 2: СОЗДАНИЕ СЕРВЕРА НА TIMEWEB CLOUD (10 минут)

### Шаг 2.1: Регистрация/вход

1. Открой https://timeweb.cloud
2. **Если нет аккаунта:**
   - Нажми **Регистрация** (справа сверху)
   - Введи email, пароль
   - Подтверди email (проверь почту)
   - Пополни баланс (минимум 300₽)
3. **Если есть аккаунт:**
   - Нажми **Войти**

---

### Шаг 2.2: Создание Cloud сервера

1. **В личном кабинете** (после входа):
   - Слева в меню найди **Cloud серверы**
   - Нажми **Создать сервер** (большая синяя кнопка)

2. **Выбор операционной системы:**
   - Найди **Ubuntu**
   - Выбери версию **22.04 LTS** (кликни на неё)
   - НЕ выбирай другие версии!

3. **Выбор тарифа:**
   - Рекомендую: **1 CPU, 2 GB RAM, 10 GB SSD**
   - Стоимость: ~300-400₽/месяц
   - Можешь взять меньше (1 GB RAM), но будет медленнее

4. **Регион:**
   - Выбери ближайший к тебе (Москва, СПб)

5. **Настройки доступа:**
   - **Root пароль:** ПРИДУМАЙ СЛОЖНЫЙ ПАРОЛЬ
   - ⚠️ ЗАПИШИ ЕГО! Понадобится много раз
   - Пример: `MySecurePass123!@#`

6. **Имя сервера:**
   - Можешь назвать: `agile-mind-pro`
   - Или любое другое (латиница, без пробелов)

7. **Нажми кнопку "Создать сервер"** (внизу страницы)

8. **Подожди 2-3 минуты** - сервер создаётся

---

### Шаг 2.3: Получи IP адрес сервера

1. **После создания** откроется страница сервера
2. **Найди блок "IP адрес"** - там будет что-то вроде `85.192.34.56`
3. **СКОПИРУЙ ЭТОТ IP** - он понадобится ВЕЗДЕ дальше
4. ⚠️ **ЗАПИШИ ЕГО** в блокнот!

✅ **Готово!** Сервер создан, IP получен.

---

## ЭТАП 3: НАСТРОЙКА DNS НА REG.RU (10 минут)

**Зачем:** Чтобы твой домен открывал твой сервер.

### Шаг 3.1: Вход в Reg.ru

1. Открой https://reg.ru
2. Нажми **Вход** (справа сверху)
3. Введи логин/пароль от аккаунта Reg.ru

---

### Шаг 3.2: Найди свой домен

1. **После входа** увидишь список доменов
2. Найди нужный домен (например, `myproject.ru`)
3. Кликни на него

---

### Шаг 3.3: Открой DNS-зону

1. **На странице домена** найди вкладку **Управление доменом**
2. Кликни **DNS-серверы и зона**
3. Убедись, что выбрано **DNS-серверы Reg.ru**
4. Кликни **Редактировать зону**

---

### Шаг 3.4: Добавь A-записи

**Что такое A-запись?** Это связь "домен → IP сервера".

**Нужно создать ДВЕ записи:**

#### Запись 1: Главный домен (@)

1. Нажми **Добавить запись** (зелёная кнопка)
2. **Тип записи:** выбери **A**
3. **Субдомен:** введи **@** (собачка)
4. **IP адрес:** вставь IP твоего сервера (из Шага 2.3)
5. **TTL:** оставь **3600** (по умолчанию)
6. Нажми **Добавить**

#### Запись 2: www субдомен

1. Снова нажми **Добавить запись**
2. **Тип записи:** выбери **A**
3. **Субдомен:** введи **www**
4. **IP адрес:** вставь ТОТ ЖЕ IP сервера
5. **TTL:** оставь **3600**
6. Нажми **Добавить**

---

### Шаг 3.5: Сохрани изменения

1. **Внизу страницы** нажми **Сохранить изменения** (большая синяя кнопка)
2. Появится сообщение "Изменения сохранены"

---

### Шаг 3.6: Подожди 10-15 минут

**Важно!** DNS-записи обновляются не мгновенно. Нужно подождать.

**Как проверить, что DNS обновился:**

1. Открой PowerShell
2. Введи:
   ```powershell
   nslookup твой-домен.ru
   ```
   (Замени `твой-домен.ru` на свой домен!)

3. **Если увидел свой IP** - DNS обновился! ✅
4. **Если не увидел** - подожди ещё 5 минут и проверь снова

✅ **Готово!** DNS настроен, можно идти дальше.

---

## ЭТАП 4: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ ПО SSH (5 минут)

**Что такое SSH?** Это способ управлять сервером из командной строки.

### Шаг 4.1: Открой PowerShell

1. Нажми **Win+R**
2. Введи **powershell**
3. Нажми **Enter**

---

### Шаг 4.2: Подключись к серверу

1. Введи команду (ЗАМЕНИ IP НА СВОЙ!):
   ```powershell
   ssh root@85.192.34.56
   ```
   (Вместо `85.192.34.56` подставь СВОЙ IP из Шага 2.3)

2. **Первый раз** увидишь вопрос:
   ```
   Are you sure you want to continue connecting (yes/no)?
   ```
   - Введи **yes** и нажми Enter

3. **Попросит пароль:**
   ```
   root@85.192.34.56's password:
   ```
   - Введи пароль root (который ты придумал в Шаге 2.2)
   - ⚠️ **ПАРОЛЬ НЕ ОТОБРАЖАЕТСЯ** при вводе - это нормально!
   - Просто введи и нажми Enter

4. **Если всё ок, увидишь:**
   ```
   root@agile-mind-pro:~#
   ```

✅ **Готово!** Ты на сервере!

**Если не получается подключиться:**
- Проверь IP адрес (скопировал правильно?)
- Проверь пароль
- Проверь, что сервер запущен в Timeweb панели

---

## ЭТАП 5: УСТАНОВКА NGINX И CERTBOT НА СЕРВЕРЕ (10 минут)

**Важно:** Все команды вводи ПО ОДНОЙ и жди завершения!

### Шаг 5.1: Обнови систему

Введи:
```bash
apt update && apt upgrade -y
```

**Что будет:**
- Появится много текста (идёт обновление)
- Подожди 1-2 минуты
- Не закрывай окно!

---

### Шаг 5.2: Установи Nginx

Введи:
```bash
apt install -y nginx
```

**Что будет:**
- Установка пакетов (~30 секунд)
- В конце увидишь приглашение `root@...#`

**Проверь, что Nginx установлен:**
```bash
nginx -v
```

Должно показать: `nginx version: nginx/1.18.0` (или похожее)

---

### Шаг 5.3: Установи Certbot (для SSL)

Введи:
```bash
apt install -y certbot python3-certbot-nginx
```

**Подожди 30-60 секунд** - установка завершится.

---

### Шаг 5.4: Настрой Firewall

**Зачем:** Открыть порты для HTTP и HTTPS.

Введи **все три команды** по очереди:

```bash
ufw allow 22/tcp
```
(Разрешаем SSH - чтобы не потерять доступ!)

```bash
ufw allow 80/tcp
```
(Разрешаем HTTP)

```bash
ufw allow 443/tcp
```
(Разрешаем HTTPS)

```bash
ufw --force enable
```
(Включаем firewall)

**Что увидишь:** `Firewall is active and enabled on system startup`

---

### Шаг 5.5: Создай папку для сайта

Введи:
```bash
mkdir -p /var/www/agile-mind-pro
```

**Что сделали:** Создали папку, куда будем загружать файлы.

---

### Шаг 5.6: Установи права на папку

Введи:
```bash
chown -R www-data:www-data /var/www/agile-mind-pro
```

**Что сделали:** Дали Nginx права на эту папку.

✅ **Готово!** Сервер подготовлен.

---

## ЭТАП 6: ЗАГРУЗКА ФАЙЛОВ НА СЕРВЕР (10 минут)

**Есть 2 способа:** через командную строку (SCP) или через программу (WinSCP).

### СПОСОБ 1: Через командную строку (SCP)

**Для продвинутых пользователей.**

1. **НЕ ЗАКРЫВАЙ** окно SSH (оставь его открытым)
2. **Открой НОВОЕ окно PowerShell** (Win+R → powershell → Enter)
3. Перейди в папку проекта:
   ```powershell
   cd C:\Users\1\agile-mind-pro\frontend
   ```

4. Используй автоматический скрипт:
   ```powershell
   .\deploy.ps1 -ServerIP 85.192.34.56
   ```
   (ЗАМЕНИ IP на свой!)

5. **Введи пароль root** когда попросит

6. Подожди 10-30 секунд - файлы загружаются

✅ **Готово!** Переходи к Шагу 7.

---

### СПОСОБ 2: Через WinSCP (для новичков) ⭐ РЕКОМЕНДУЮ

**Проще и понятнее!**

#### Шаг 6.2.1: Скачай WinSCP

1. Открой https://winscp.net/eng/download.php
2. Нажми **Download WinSCP**
3. Установи программу (Next → Next → Install)

---

#### Шаг 6.2.2: Подключись к серверу

1. **Запусти WinSCP**
2. Появится окно **Вход**:
   - **Протокол:** SFTP
   - **Имя хоста:** твой IP (например, `85.192.34.56`)
   - **Порт:** 22
   - **Имя пользователя:** root
   - **Пароль:** твой root пароль

3. Нажми **Вход**

4. **Если первый раз**, появится предупреждение:
   - Нажми **Да** (добавить в известные хосты)

5. **Откроется два окна:**
   - **Слева:** твой компьютер
   - **Справа:** сервер

---

#### Шаг 6.2.3: Найди папку dist

**Слева (твой компьютер):**

1. Перейди в `C:\Users\1\agile-mind-pro\frontend\`
2. Найди папку **dist**
3. Открой её (двойной клик)

---

#### Шаг 6.2.4: Найди папку на сервере

**Справа (сервер):**

1. В строке пути введи: `/var/www/agile-mind-pro`
2. Нажми Enter

---

#### Шаг 6.2.5: Загрузи файлы

1. **Слева в dist/** выдели ВСЕ файлы (Ctrl+A)
2. **Перетащи их вправо** (в окно сервера)
3. Появится окно копирования - нажми **Копировать**
4. **Подожди 10-30 секунд** - файлы загружаются

**Проверь:** Справа должны появиться файлы (index.html, assets/, и т.д.)

✅ **Готово!** Файлы на сервере. Можешь закрыть WinSCP.

---

## ЭТАП 7: НАСТРОЙКА NGINX (10 минут)

**Вернись в окно SSH** (где был подключен к серверу).

### Шаг 7.1: Создай конфиг Nginx

Введи:
```bash
nano /etc/nginx/sites-available/agile-mind-pro
```

**Откроется редактор nano.**

---

### Шаг 7.2: Вставь конфигурацию

**Скопируй ВЕСь этот текст** (ЗАМЕНИ домен на свой!):

```nginx
server {
    listen 80;
    server_name твой-домен.ru www.твой-домен.ru;

    root /var/www/agile-mind-pro;
    index index.html;

    # SPA routing (важно для React!)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Кэширование статики
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

**⚠️ ВАЖНО:** Замени `твой-домен.ru` на СВОЙ домен в двух местах!

---

### Шаг 7.3: Сохрани файл

1. Нажми **Ctrl+O** (буква O, не ноль)
2. Нажми **Enter** (подтвердить имя файла)
3. Нажми **Ctrl+X** (выйти из nano)

---

### Шаг 7.4: Активируй конфиг

Введи:
```bash
ln -s /etc/nginx/sites-available/agile-mind-pro /etc/nginx/sites-enabled/
```

**Что сделали:** Создали символическую ссылку (включили конфиг).

---

### Шаг 7.5: Удали дефолтный конфиг

Введи:
```bash
rm /etc/nginx/sites-enabled/default
```

**Зачем:** Чтобы не конфликтовал с нашим конфигом.

---

### Шаг 7.6: Проверь конфиг на ошибки

Введи:
```bash
nginx -t
```

**Должно показать:**
```
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

**Если показало ошибку:**
- Проверь, правильно ли скопировал конфиг
- Проверь, заменил ли домен
- Повтори Шаг 7.1-7.3

---

### Шаг 7.7: Перезапусти Nginx

Введи:
```bash
systemctl restart nginx
```

**Подожди 2-3 секунды.**

**Проверь статус:**
```bash
systemctl status nginx
```

**Должно быть:** `Active: active (running)` зелёным цветом

Нажми **Q** чтобы выйти.

✅ **Готово!** Nginx настроен.

---

## ЭТАП 8: ПРОВЕРКА САЙТА (2 минуты)

### Шаг 8.1: Открой сайт по HTTP

1. Открой браузер
2. Введи в адресной строке: **http://твой-домен.ru**
   (Замени на свой домен!)

**Что должно произойти:**
- ✅ Сайт открывается
- ✅ Видна страница логина
- ✅ Можно залогиниться

**Если НЕ открывается:**
- Проверь, что DNS обновился (Шаг 3.6)
- Проверь, что файлы загружены (Шаг 6)
- Проверь, что Nginx запущен (Шаг 7.7)

✅ **Если открылся - ОТЛИЧНО!** Переходим к SSL.

---

## ЭТАП 9: НАСТРОЙКА SSL (HTTPS) (5 минут)

**Зачем:** Чтобы был зелёный замочек и HTTPS.

### Шаг 9.1: Запусти Certbot

**Вернись в SSH окно.**

Введи (ЗАМЕНИ домен на свой!):
```bash
certbot --nginx -d твой-домен.ru -d www.твой-домен.ru
```

---

### Шаг 9.2: Ответь на вопросы Certbot

**1. Email адрес:**
```
Enter email address (used for urgent renewal and security notices):
```
- Введи свой email
- Нажми Enter

**2. Согласие с ToS:**
```
Please read the Terms of Service...
(A)gree/(C)ancel:
```
- Введи **A**
- Нажми Enter

**3. Подписка на рассылку:**
```
Would you be willing to share your email...
(Y)es/(N)o:
```
- Введи **N** (можешь и Y, не важно)
- Нажми Enter

**4. Редирект на HTTPS:**
```
Please choose whether or not to redirect HTTP traffic to HTTPS...
1: No redirect
2: Redirect
Select the appropriate number [1-2]:
```
- Введи **2** (ОБЯЗАТЕЛЬНО!)
- Нажми Enter

---

### Шаг 9.3: Подожди установки

**Certbot будет:**
1. Проверять домен (10 секунд)
2. Получать сертификат (20 секунд)
3. Настраивать Nginx (5 секунд)

**В конце увидишь:**
```
Congratulations! You have successfully enabled https://...
```

✅ **УРА!** SSL установлен!

---

### Шаг 9.4: Проверь HTTPS

1. Открой браузер
2. Введи: **https://твой-домен.ru** (с https!)

**Что должно быть:**
- ✅ Зелёный замочек в адресной строке
- ✅ Сайт открывается
- ✅ Всё работает

---

## 🎉 ПОЗДРАВЛЯЮ! ВСЁ ГОТОВО!

Твой сайт развёрнут на:
- **HTTP:** http://твой-домен.ru (редиректит на HTTPS)
- **HTTPS:** https://твой-домен.ru ✅

---

## 🔄 КАК ОБНОВИТЬ САЙТ В БУДУЩЕМ

**Когда изменишь код и нужно обновить сайт:**

### Способ 1: Автоматический скрипт (ЛЕГКО)

**На своём компьютере в PowerShell:**

```powershell
cd C:\Users\1\agile-mind-pro\frontend
.\deploy.ps1 -ServerIP 85.192.34.56
```

(Замени IP на свой!)

**Введи пароль root - готово!**

---

### Способ 2: Вручную

1. Собери проект: `npm run build`
2. Открой WinSCP
3. Подключись к серверу
4. Удали старые файлы в `/var/www/agile-mind-pro/`
5. Загрузи новые из `dist/`

**Nginx перезапускать НЕ НУЖНО!**

---

## ❓ ЧАСТЫЕ ПРОБЛЕМЫ

### "Сайт не открывается"

**Проверь:**
1. DNS обновился? `nslookup твой-домен.ru`
2. Nginx запущен? `systemctl status nginx` (на сервере)
3. Файлы загружены? Проверь `/var/www/agile-mind-pro/` (должен быть index.html)
4. Firewall открыл порты? `ufw status` (на сервере)

---

### "SSL не работает / нет замочка"

**Проверь:**
1. Certbot успешно завершился?
2. Перешёл на HTTPS (не HTTP)?
3. Попробуй Hard Refresh: Ctrl+Shift+R

---

### "403 Forbidden"

**Решение:**
```bash
chown -R www-data:www-data /var/www/agile-mind-pro
chmod -R 755 /var/www/agile-mind-pro
systemctl restart nginx
```

---

### "502 Bad Gateway"

**Решение:**
```bash
systemctl restart nginx
```

Если не помогло - проверь логи:
```bash
tail -f /var/log/nginx/error.log
```

---

### "Не могу подключиться по SSH"

**Проверь:**
1. Правильный IP?
2. Правильный пароль?
3. Сервер запущен? (в Timeweb панели)
4. Firewall разрешает порт 22? (должен быть открыт)

---

## 📞 НУЖНА ПОМОЩЬ?

**Если застрял:**
1. Прочитай раздел "Частые проблемы" выше
2. Проверь логи Nginx: `tail -f /var/log/nginx/error.log`
3. Перечитай шаг, на котором возникла проблема
4. Попробуй начать с начала (удали и пересоздай сервер)

---

## ✅ ЧЕКЛИСТ ФИНАЛЬНОЙ ПРОВЕРКИ

Пройдись по списку:

- [ ] Сайт открывается по HTTPS
- [ ] Зелёный замочек в браузере
- [ ] Логин работает
- [ ] Доски создаются
- [ ] Задачи работают
- [ ] Комментарии добавляются
- [ ] Файлы загружаются
- [ ] Всё работает на телефоне
- [ ] DNS обновился
- [ ] Firestore Rules применены
- [ ] SSL сертификат установлен
- [ ] Nginx запущен

**Если всё ✅ - ГОТОВО! Можно пользоваться!**

---

🎊 **Молодец, что дошёл до конца!**

Теперь у тебя production-ready приложение на своём домене с SSL!

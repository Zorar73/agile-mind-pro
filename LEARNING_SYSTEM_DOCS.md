# Документация: Система Обучения (Learning Management System)

## Оглавление
1. [Архитектура](#архитектура)
2. [Структура данных](#структура-данных)
3. [Сервисы и API](#сервисы-и-api)
4. [Компоненты (Страницы)](#компоненты-страницы)
5. [Пользовательские сценарии](#пользовательские-сценарии)
6. [Навигация](#навигация)

---

## Архитектура

### Общая схема

```
┌─────────────────────────────────────────────────────────┐
│                    FIRESTORE DATABASE                    │
│                                                           │
│  ┌──────────┐   ┌──────────┐   ┌───────────────┐       │
│  │ courses  │   │ lessons  │   │ user_progress │       │
│  └──────────┘   └──────────┘   └───────────────┘       │
└─────────────────────────────────────────────────────────┘
                          ▲
                          │
                          │ Firebase SDK
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              learning.service.js (API Layer)             │
│  • createCourse          • getCourseLessons              │
│  • updateCourse          • markLessonCompleted           │
│  • deleteCourse          • getUserCourseProgress         │
│  • createLesson          • getUserCoursesWithProgress    │
│  • updateLesson                                           │
│  • deleteLesson                                           │
└─────────────────────────────────────────────────────────┘
                          ▲
                          │
                          │ React Components
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                    │
│                                                           │
│  Студенты:                    Администраторы:            │
│  • LearningPortalPage        • LearningAdminPage         │
│  • CoursePage                • LessonManagementPage      │
│  • LessonPage                                             │
└─────────────────────────────────────────────────────────┘
```

---

## Структура данных

### Коллекция: `courses`

Хранит информацию о курсах.

**Документ курса:**
```javascript
{
  id: "auto_generated_id",
  title: "Введение в Agile Mind Pro",
  description: "Базовый курс для новых пользователей",
  category: "getting-started", // getting-started, boards, sprints, teams, analytics, ai, general
  thumbnail: "https://...",     // URL изображения (опционально)
  duration: "2 часа",           // Строка, примерное время
  requiredRole: "all",          // all, member, admin - кто может видеть курс
  createdBy: "user_uid",
  createdAt: Timestamp,
  updatedAt: Timestamp,
  lessonsCount: 5,              // Автоматически обновляется при добавлении/удалении уроков
  studentsCount: 0              // Для будущего функционала
}
```

**Категории курсов:**
- `getting-started` - Начало работы
- `boards` - Доски
- `sprints` - Спринты
- `teams` - Команды
- `analytics` - Аналитика
- `ai` - AI Ассистент
- `general` - Общее

---

### Коллекция: `lessons`

Хранит уроки, связанные с курсами.

**Документ урока:**
```javascript
{
  id: "auto_generated_id",
  courseId: "course_id_reference",   // Ссылка на курс
  title: "Как создать свою первую доску",
  content: "Текстовое содержимое урока...\n\nМожет быть многострочным",
  videoUrl: "https://youtube.com/embed/...",  // URL для iframe (опционально)
  type: "video",                     // "video" или "article"
  duration: "15 мин",
  order: 0,                          // Порядок урока в курсе (0, 1, 2, ...)
  createdBy: "user_uid",
  createdAt: Timestamp,
  updatedAt: Timestamp
}
```

**Типы уроков:**
- `video` - Видео-урок (требует videoUrl)
- `article` - Текстовый урок (только content)

---

### Коллекция: `user_progress`

Хранит прогресс пользователей по курсам.

**ID документа:** `${userId}_${courseId}`
Например: `abc123_xyz789`

**Документ прогресса:**
```javascript
{
  id: "userId_courseId",
  userId: "user_uid",
  courseId: "course_id",
  completedLessons: ["lesson_id_1", "lesson_id_2", "lesson_id_3"],  // Массив ID пройденных уроков
  progress: 60,                    // Процент выполнения (0-100)
  startedAt: Timestamp,            // Когда пользователь начал курс
  completedAt: Timestamp | null,   // Когда завершил (null если не завершен)
  updatedAt: Timestamp
}
```

**Расчет прогресса:**
```javascript
progress = Math.round((completedLessons.length / totalLessons) * 100)
```

---

## Сервисы и API

### `learning.service.js`

Центральный сервис для работы с системой обучения.

#### Методы работы с курсами

##### `createCourse(courseData, userId)`
Создает новый курс.

**Параметры:**
```javascript
{
  title: string,           // Обязательно
  description: string,
  category: string,
  thumbnail: string,
  duration: string,
  requiredRole: string     // "all", "member", "admin"
}
```

**Возвращает:**
```javascript
{ success: true, courseId: "..." }
// или
{ success: false, error: "..." }
```

---

##### `getAllCourses()`
Получает все курсы, отсортированные по дате создания (новые первыми).

**Возвращает:**
```javascript
{
  success: true,
  courses: [
    { id, title, description, category, ... },
    ...
  ]
}
```

---

##### `getCourse(courseId)`
Получает один курс по ID.

**Возвращает:**
```javascript
{ success: true, course: { id, title, ... } }
```

---

##### `updateCourse(courseId, updates)`
Обновляет курс.

**Параметры:**
```javascript
{
  title: string,
  description: string,
  // ... любые поля для обновления
}
```

---

##### `deleteCourse(courseId)`
Удаляет курс и все его уроки.

**Процесс:**
1. Находит все уроки курса
2. Удаляет все уроки
3. Удаляет сам курс

---

#### Методы работы с уроками

##### `createLesson(lessonData, userId)`
Создает новый урок и увеличивает счетчик `lessonsCount` в курсе.

**Параметры:**
```javascript
{
  courseId: string,       // Обязательно
  title: string,          // Обязательно
  content: string,
  videoUrl: string,       // Обязательно для type === 'video'
  type: string,           // "video" или "article"
  duration: string,
  order: number
}
```

---

##### `getCourseLessons(courseId)`
Получает все уроки курса, отсортированные по `order`.

**Возвращает:**
```javascript
{
  success: true,
  lessons: [
    { id, title, content, order, ... },
    ...
  ]
}
```

---

##### `getLesson(lessonId)`
Получает один урок по ID.

---

##### `updateLesson(lessonId, updates)`
Обновляет урок.

---

##### `deleteLesson(lessonId, courseId)`
Удаляет урок и уменьшает счетчик `lessonsCount` в курсе.

---

#### Методы работы с прогрессом

##### `getUserCourseProgress(userId, courseId)`
Получает прогресс пользователя по конкретному курсу.

**Возвращает:**
```javascript
{
  success: true,
  progress: {
    userId,
    courseId,
    completedLessons: ["lesson1", "lesson2"],
    progress: 40,
    startedAt: Date,
    completedAt: null
  }
}
```

Если прогресса нет, возвращает пустой объект прогресса.

---

##### `markLessonCompleted(userId, courseId, lessonId)`
Отмечает урок как пройденный.

**Процесс:**
1. Получает или создает документ прогресса
2. Добавляет `lessonId` в массив `completedLessons` (если его там еще нет)
3. Получает общее количество уроков курса
4. Вычисляет процент: `(completedLessons.length / totalLessons) * 100`
5. Если прогресс = 100%, устанавливает `completedAt`
6. Сохраняет документ прогресса

**Возвращает:**
```javascript
{ success: true, progress: 100 }
```

---

##### `getUserCoursesWithProgress(userId)`
Получает все курсы с прогрессом пользователя.

**Процесс:**
1. Получает все курсы
2. Получает все документы прогресса пользователя
3. Создает мапу `courseId -> progress`
4. Объединяет курсы с прогрессом

**Возвращает:**
```javascript
{
  success: true,
  courses: [
    {
      id,
      title,
      description,
      // ... поля курса
      userProgress: {
        completedLessons: [...],
        progress: 60,
        startedAt: Date,
        completedAt: null
      }
    },
    ...
  ]
}
```

Если у пользователя нет прогресса по курсу, `userProgress` будет пустым объектом:
```javascript
userProgress: {
  completedLessons: [],
  progress: 0,
  startedAt: null,
  completedAt: null
}
```

---

## Компоненты (Страницы)

### Для студентов

#### `LearningPortalPage.jsx`
**Путь:** `/learning`

Главный портал обучения для пользователей.

**Что отображает:**
1. **Hero-секция:**
   - Заголовок "Портал обучения"
   - Кнопка "Управление курсами" (только для админов)

2. **Статистика (3 карточки):**
   - Всего курсов
   - Завершено курсов
   - В процессе

3. **Поиск:**
   - Поле поиска по названию и описанию курсов

4. **Табы категорий:**
   - Все / Начало работы / Доски / Спринты / ...
   - Фильтрует курсы по категориям

5. **Сетка курсов (Grid):**
   - Карточки курсов с превью
   - Прогресс-бар для начатых курсов
   - Бейдж "Завершено" / "В процессе"

**Данные:**
```javascript
const loadCourses = async () => {
  const result = await learningService.getUserCoursesWithProgress(user.uid);
  // result.courses содержит курсы с userProgress
};
```

**Логика:**
- При клике на курс → переход на `/learning/course/:courseId`
- Админы видят кнопку "Управление курсами" → `/learning/admin`

---

#### `CoursePage.jsx`
**Путь:** `/learning/course/:courseId`

Страница курса со списком уроков.

**Что отображает:**
1. **Шапка курса:**
   - Превью-изображение (или градиент)
   - Название и описание
   - Чипы: длительность, количество уроков, статус "Завершено"
   - Прогресс-бар

2. **Список уроков:**
   - Номер урока (1, 2, 3, ...)
   - Название
   - Тип (видео/статья)
   - Длительность
   - Иконка галочки если пройден

**Данные:**
```javascript
const loadData = async () => {
  const courseResult = await learningService.getCourse(courseId);
  const lessonsResult = await learningService.getCourseLessons(courseId);
  const progressResult = await learningService.getUserCourseProgress(user.uid, courseId);
};
```

**Логика:**
- При клике на урок → переход на `/learning/lesson/:lessonId`
- Кнопка "Вернуться к курсам" → `/learning`

---

#### `LessonPage.jsx`
**Путь:** `/learning/lesson/:lessonId`

Страница отдельного урока.

**Что отображает:**
1. **Шапка урока:**
   - Чип с названием курса
   - Номер урока (X из Y)
   - Бейдж "Пройдено" если завершен
   - Название урока
   - Длительность

2. **Видео (если type === 'video'):**
   - iframe с видео (YouTube/Vimeo)
   - Aspect ratio 16:9

3. **Содержание:**
   - Текст урока (с сохранением переносов строк)

4. **Кнопки действий:**
   - "Отметить как пройденный" (если еще не пройден)
   - "Следующий урок" / "Завершить курс"

**Данные:**
```javascript
const loadData = async () => {
  const lessonResult = await learningService.getLesson(lessonId);
  const courseResult = await learningService.getCourse(lesson.courseId);
  const lessonsResult = await learningService.getCourseLessons(lesson.courseId);
  const progressResult = await learningService.getUserCourseProgress(user.uid, lesson.courseId);
};
```

**Логика отметки как пройденный:**
```javascript
const handleMarkComplete = async () => {
  const result = await learningService.markLessonCompleted(user.uid, lesson.courseId, lessonId);

  if (result.success) {
    setIsCompleted(true);
    toast.success('Урок отмечен как пройденный');

    if (result.progress === 100) {
      toast.success('🎉 Поздравляем! Вы завершили курс!');
    }
  }
};
```

**Логика перехода к следующему уроку:**
```javascript
const handleNextLesson = () => {
  const currentIndex = allLessons.findIndex(l => l.id === lessonId);

  if (currentIndex < allLessons.length - 1) {
    // Есть следующий урок
    navigate(`/learning/lesson/${allLessons[currentIndex + 1].id}`);
  } else {
    // Последний урок - возвращаемся к курсу
    navigate(`/learning/course/${lesson.courseId}`);
  }
};
```

---

### Для администраторов

#### `LearningAdminPage.jsx`
**Путь:** `/learning/admin`

Панель управления курсами (только для админов).

**Что отображает:**
1. **Заголовок:**
   - Кнопка "Создать курс"

2. **Сетка курсов (Grid):**
   - Карточки с названием, описанием, категорией
   - Количество уроков
   - Кнопки: Редактировать, Удалить
   - Кнопка "Управление уроками"

3. **Диалог создания/редактирования курса:**
   - Название
   - Описание (multiline)
   - Категория (select)
   - Длительность
   - Кому доступен (select: Всем / Только участникам / Только админам)

**Данные:**
```javascript
const loadCourses = async () => {
  const result = await learningService.getAllCourses();
  setCourses(result.courses);
};
```

**Логика:**
- Создать курс → `learningService.createCourse(formData, user.uid)`
- Обновить курс → `learningService.updateCourse(courseId, formData)`
- Удалить курс → `learningService.deleteCourse(courseId)` (с подтверждением)
- "Управление уроками" → `/learning/admin/course/:courseId`

---

#### `LessonManagementPage.jsx`
**Путь:** `/learning/admin/course/:courseId`

Панель управления уроками курса.

**Что отображает:**
1. **Заголовок:**
   - Название курса
   - Кнопка "Добавить урок"

2. **Список уроков:**
   - Кнопки переупорядочивания (вверх/вниз)
   - Номер, название, тип, длительность
   - Превью содержания
   - Кнопки: Редактировать, Удалить

3. **Диалог создания/редактирования урока:**
   - Название урока
   - Тип (select: Статья / Видео)
   - URL видео (показывается только для типа "Видео")
   - Содержание (multiline, 8 строк)
   - Длительность

**Данные:**
```javascript
const loadData = async () => {
  const courseResult = await learningService.getCourse(courseId);
  const lessonsResult = await learningService.getCourseLessons(courseId);
};
```

**Логика переупорядочивания:**
```javascript
const handleMoveLesson = async (lessonId, direction) => {
  // 1. Находим текущий индекс урока
  const currentIndex = lessons.findIndex(l => l.id === lessonId);

  // 2. Вычисляем новый индекс
  const newIndex = direction === 'up' ? currentIndex - 1 : currentIndex + 1;

  // 3. Переставляем в массиве
  const updatedLessons = [...lessons];
  const [movedLesson] = updatedLessons.splice(currentIndex, 1);
  updatedLessons.splice(newIndex, 0, movedLesson);

  // 4. Обновляем order для всех затронутых уроков
  const updatePromises = updatedLessons.map((lesson, index) => {
    if (lesson.order !== index) {
      return learningService.updateLesson(lesson.id, { order: index });
    }
  });

  await Promise.all(updatePromises);
  loadData(); // Перезагружаем
};
```

**Логика:**
- Создать урок → `learningService.createLesson({ ...formData, courseId }, user.uid)`
- Обновить урок → `learningService.updateLesson(lessonId, formData)`
- Удалить урок → `learningService.deleteLesson(lessonId, courseId)`
- Переместить вверх/вниз → обновляет `order` у затронутых уроков

---

## Пользовательские сценарии

### Сценарий 1: Студент проходит курс

1. **Открывает портал обучения:**
   - Переходит: Боковое меню → "Обучение"
   - URL: `/learning`
   - Видит все доступные курсы и свою статистику

2. **Выбирает категорию (опционально):**
   - Кликает на таб "Начало работы"
   - Курсы фильтруются

3. **Открывает курс:**
   - Кликает на карточку курса
   - URL: `/learning/course/abc123`
   - Видит список всех уроков, свой прогресс

4. **Начинает урок:**
   - Кликает на первый урок
   - URL: `/learning/lesson/xyz789`
   - Смотрит видео или читает текст

5. **Отмечает урок как пройденный:**
   - Кликает "Отметить как пройденный"
   - Урок добавляется в `user_progress.completedLessons`
   - Прогресс пересчитывается
   - Тост: "Урок отмечен как пройденный"

6. **Переходит к следующему уроку:**
   - Кликает "Следующий урок"
   - Автоматически открывается следующий урок

7. **Завершает курс:**
   - Отмечает последний урок
   - Прогресс становится 100%
   - `user_progress.completedAt` устанавливается
   - Тост: "🎉 Поздравляем! Вы завершили курс!"

8. **Возвращается к списку курсов:**
   - На курсе появляется бейдж "Завершено"
   - Статистика обновляется: "Завершено: 1"

---

### Сценарий 2: Администратор создает курс

1. **Открывает портал обучения:**
   - URL: `/learning`
   - Видит кнопку "Управление курсами" (т.к. role === 'admin')

2. **Переходит в админ-панель:**
   - Кликает "Управление курсами"
   - URL: `/learning/admin`

3. **Создает новый курс:**
   - Кликает "Создать курс"
   - Заполняет форму:
     - Название: "Работа с досками"
     - Описание: "Полное руководство..."
     - Категория: "Доски"
     - Длительность: "1 час"
     - Кому доступен: "Всем"
   - Кликает "Создать"
   - Firestore: создается документ в `courses`
   - Тост: "Курс создан"

4. **Добавляет уроки:**
   - Кликает "Управление уроками" на карточке курса
   - URL: `/learning/admin/course/abc123`
   - Кликает "Добавить урок"

5. **Создает первый урок (видео):**
   - Заполняет форму:
     - Название: "Как создать доску"
     - Тип: "Видео"
     - URL видео: "https://youtube.com/embed/xyz..."
     - Содержание: "В этом уроке вы узнаете..."
     - Длительность: "10 мин"
   - Кликает "Создать"
   - Firestore: создается документ в `lessons` с `order: 0`
   - `courses.lessonsCount` увеличивается на 1
   - Тост: "Урок создан"

6. **Создает второй урок (статья):**
   - Кликает "Добавить урок"
   - Тип: "Статья"
   - Без URL видео
   - Firestore: создается с `order: 1`

7. **Переупорядочивает уроки:**
   - Решает поменять местами уроки 1 и 2
   - Кликает стрелку "вниз" на первом уроке
   - Firestore: обновляется `order` у обоих уроков
   - Список перестраивается

8. **Публикация:**
   - Курс автоматически виден всем пользователям
   - Появляется в портале обучения

---

## Навигация

### Структура маршрутов

```
/learning                              → LearningPortalPage (портал для студентов)
  ├─ /learning/course/:courseId        → CoursePage (детали курса)
  │   └─ /learning/lesson/:lessonId    → LessonPage (просмотр урока)
  │
  └─ /learning/admin                   → LearningAdminPage (управление курсами)
      └─ /learning/admin/course/:courseId  → LessonManagementPage (управление уроками)
```

### Кнопки навигации

**На каждой странице:**

| Страница | Кнопка "Назад" | Куда ведет |
|----------|----------------|------------|
| LearningPortalPage | Нет | - |
| CoursePage | "Вернуться к курсам" | `/learning` |
| LessonPage | "Вернуться к курсу" | `/learning/course/:courseId` |
| LearningAdminPage | "Вернуться к курсам" | `/learning` |
| LessonManagementPage | "Вернуться к управлению курсами" | `/learning/admin` |

### Хлебные крошки

При навигации по системе обучения в AppBar отображаются хлебные крошки:

```
Главная > Обучение
```

Конфигурация в `MainLayout.jsx`:
```javascript
'/learning': { label: 'Обучение', icon: School, color: bauhaus.teal }
```

### Боковое меню

Пункт "Обучение" находится в основном меню между "Наброски" и "Новости".

**Код в `Sidebar.jsx`:**
```javascript
{ title: 'Обучение', icon: <School />, path: '/learning', color: bauhaus.teal }
```

---

## Технические детали

### Доступ по ролям

**Просмотр курсов:**
- Определяется полем `course.requiredRole`
- `"all"` - виден всем
- `"member"` - виден только участникам команды
- `"admin"` - виден только админам

**Реализация:**
- Фильтрация на уровне UI (пока не реализовано)
- В будущем: query с `where('requiredRole', '==', userRole)`

**Управление курсами:**
```javascript
const isAdmin = user?.role === 'admin' || user?.role === 'owner';

{isAdmin && (
  <Button onClick={() => navigate('/learning/admin')}>
    Управление курсами
  </Button>
)}
```

---

### Обработка состояний

#### Загрузка (Loading)

Все страницы показывают `<CircularProgress />` во время загрузки:

```javascript
if (loading) {
  return (
    <MainLayout>
      <Box sx={{ display: 'flex', justifyContent: 'center', minHeight: '60vh' }}>
        <CircularProgress />
      </Box>
    </MainLayout>
  );
}
```

#### Пустые состояния (Empty States)

**Нет курсов:**
```javascript
{courses.length === 0 && (
  <Card sx={{ p: 6, textAlign: 'center' }}>
    <School sx={{ fontSize: 64, color: 'text.disabled' }} />
    <Typography>Курсы еще не добавлены</Typography>
    {isAdmin && (
      <Button onClick={() => navigate('/learning/admin')}>
        Создать первый курс
      </Button>
    )}
  </Card>
)}
```

#### Ошибки

В случае ошибок API возвращает:
```javascript
{ success: false, error: "Error message" }
```

Обработка в UI:
```javascript
if (result.success) {
  toast.success('Операция выполнена');
} else {
  toast.error('Ошибка выполнения операции');
}
```

---

### Оптимизация

#### Кэширование

- Нет встроенного кэширования
- При каждом переходе загружаются свежие данные
- Можно добавить React Query для кэширования

#### Подгрузка данных

Все страницы используют паттерн:
```javascript
useEffect(() => {
  if (user && courseId) {
    loadData();
  }
}, [user, courseId]);
```

---

## Примеры данных

### Пример полного потока данных

**1. Админ создает курс "Введение в систему"**

Firestore `courses/abc123`:
```json
{
  "title": "Введение в систему",
  "description": "Базовый курс для новичков",
  "category": "getting-started",
  "duration": "1 час",
  "requiredRole": "all",
  "createdBy": "admin_uid",
  "createdAt": "2025-01-15T10:00:00Z",
  "updatedAt": "2025-01-15T10:00:00Z",
  "lessonsCount": 0
}
```

**2. Админ добавляет 3 урока**

Firestore `lessons/lesson1`:
```json
{
  "courseId": "abc123",
  "title": "Первые шаги",
  "content": "Добро пожаловать...",
  "type": "article",
  "duration": "5 мин",
  "order": 0,
  "createdBy": "admin_uid",
  "createdAt": "2025-01-15T10:05:00Z"
}
```

Firestore `lessons/lesson2`:
```json
{
  "courseId": "abc123",
  "title": "Создание доски",
  "content": "Инструкция...",
  "videoUrl": "https://youtube.com/embed/xyz",
  "type": "video",
  "duration": "10 мин",
  "order": 1
}
```

Firestore `lessons/lesson3`:
```json
{
  "courseId": "abc123",
  "title": "Первая задача",
  "content": "Теперь создадим задачу...",
  "type": "article",
  "duration": "8 мин",
  "order": 2
}
```

Firestore `courses/abc123` обновлен:
```json
{
  ...,
  "lessonsCount": 3,
  "updatedAt": "2025-01-15T10:15:00Z"
}
```

**3. Пользователь user123 начинает курс**

Открывает первый урок → `markLessonCompleted(user123, abc123, lesson1)`

Firestore `user_progress/user123_abc123`:
```json
{
  "userId": "user123",
  "courseId": "abc123",
  "completedLessons": ["lesson1"],
  "progress": 33,
  "startedAt": "2025-01-15T14:00:00Z",
  "completedAt": null,
  "updatedAt": "2025-01-15T14:05:00Z"
}
```

**4. Пользователь завершает курс**

Отмечает lesson2 и lesson3 → `markLessonCompleted` для каждого

Firestore `user_progress/user123_abc123`:
```json
{
  "userId": "user123",
  "courseId": "abc123",
  "completedLessons": ["lesson1", "lesson2", "lesson3"],
  "progress": 100,
  "startedAt": "2025-01-15T14:00:00Z",
  "completedAt": "2025-01-15T14:30:00Z",
  "updatedAt": "2025-01-15T14:30:00Z"
}
```

**5. getUserCoursesWithProgress(user123) возвращает:**

```javascript
{
  success: true,
  courses: [
    {
      id: "abc123",
      title: "Введение в систему",
      description: "Базовый курс для новичков",
      category: "getting-started",
      duration: "1 час",
      lessonsCount: 3,
      userProgress: {
        completedLessons: ["lesson1", "lesson2", "lesson3"],
        progress: 100,
        startedAt: Date("2025-01-15T14:00:00Z"),
        completedAt: Date("2025-01-15T14:30:00Z")
      }
    }
  ]
}
```

---

## Будущие улучшения

### Возможные доработки:

1. **Квизы и тесты:**
   - Добавить тип урока `"quiz"`
   - Хранить вопросы и ответы
   - Проверка знаний перед завершением курса

2. **Сертификаты:**
   - Генерация PDF сертификата при завершении курса
   - Хранение в Firestore Storage

3. **Комментарии к урокам:**
   - Пользователи могут задавать вопросы
   - Преподаватели отвечают

4. **Рейтинг курсов:**
   - Пользователи ставят оценки
   - Средний рейтинг отображается на карточке

5. **Закладки:**
   - Пользователь может сохранить урок для повторного просмотра

6. **Статистика для админов:**
   - Сколько пользователей прошли курс
   - Средняя оценка
   - Проблемные уроки (где пользователи застревают)

7. **Уведомления:**
   - Напоминания о незавершенных курсах
   - Новые курсы в интересующих категориях

8. **Дедлайны:**
   - Админ может установить дедлайн на курс
   - Пользователи получают напоминания

---

## Заключение

Система обучения полностью функциональна и готова к использованию. Она позволяет:

- ✅ Администраторам создавать курсы и уроки
- ✅ Пользователям проходить обучение с отслеживанием прогресса
- ✅ Автоматически рассчитывать прогресс
- ✅ Поддерживать видео и текстовые уроки
- ✅ Организовывать контент по категориям
- ✅ Управлять доступом по ролям

Все компоненты интегрированы в основное приложение через боковое меню "Обучение".

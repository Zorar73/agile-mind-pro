# Руководство по деплою Agile Mind Pro (вручную)

## 📋 Информация о сервере

- **Хостинг:** Timeweb Cloud
- **IP:** 89.23.98.91
- **SSH:** `ssh root@89.23.98.91`
- **Путь проекта:** `/var/www/agile-mind-pro/`
- **Веб-сервер:** Nginx

---

## 🚀 Быстрый деплой (5 минут)

### Шаг 1: Закоммитьте изменения локально

```bash
cd C:\Users\1\agile-mind-pro\frontend

# Проверьте статус
git status

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Описание изменений"

# Отправьте в GitHub
git push origin main
```

### Шаг 2: Обновите код на сервере

```bash
# Подключитесь к серверу
ssh root@89.23.98.91

# Перейдите в папку проекта
cd /var/www/agile-mind-pro

# Клонируйте свежий код во временную папку
rm -rf temp-build
git clone https://github.com/Zorar73/agile-mind-pro.git temp-build

# Перейдите в папку
cd temp-build

# Установите зависимости
npm install

# Соберите проект
npm run build

# Скопируйте файлы на место
cp -r dist/* /var/www/agile-mind-pro/
chown -R www-data:www-data /var/www/agile-mind-pro/*

# Очистите временную папку
cd ..
rm -rf temp-build

# Перезагрузите nginx
nginx -s reload

# Готово!
exit
```

---

## 📝 Пошаговая инструкция (подробно)

### 1. Подготовка на локальной машине

#### 1.1. Проверьте изменения

```bash
cd C:\Users\1\agile-mind-pro\frontend
git status
```

Вы увидите список измененных файлов.

#### 1.2. Добавьте файлы в Git

```bash
# Добавить все измененные файлы
git add .

# Или добавить конкретные файлы
git add src/pages/NewPage.jsx
git add src/services/new.service.js
```

#### 1.3. Создайте коммит

```bash
git commit -m "Описание изменений"
```

Примеры хороших сообщений коммита:
- `"Fix: Исправлена ошибка с обложками курсов"`
- `"Feat: Добавлена система уведомлений"`
- `"Update: Улучшен интерфейс экзаменов"`

#### 1.4. Отправьте в GitHub

```bash
git push origin main
```

Если просит ввести логин/пароль - используйте Personal Access Token от GitHub.

---

### 2. Обновление на сервере

#### 2.1. Подключитесь по SSH

```bash
ssh root@89.23.98.91
```

Введите пароль от сервера (вы его знаете).

#### 2.2. Получите код из GitHub

```bash
cd /var/www/agile-mind-pro

# Удалите старую временную папку (если есть)
rm -rf temp-build

# Клонируйте репозиторий
git clone https://github.com/Zorar73/agile-mind-pro.git temp-build
```

#### 2.3. Установите зависимости

```bash
cd temp-build
npm install
```

Процесс займет ~30 секунд.

#### 2.4. Соберите проект

```bash
npm run build
```

Это создаст папку `dist/` с оптимизированными файлами.
Процесс займет ~10 секунд.

#### 2.5. Скопируйте файлы

```bash
# Скопируйте файлы из dist/ в корень
cp -r dist/* /var/www/agile-mind-pro/

# Установите правильные права доступа
chown -R www-data:www-data /var/www/agile-mind-pro/*
```

#### 2.6. Очистите временные файлы

```bash
cd /var/www/agile-mind-pro
rm -rf temp-build
```

#### 2.7. Перезагрузите Nginx

```bash
nginx -s reload
```

#### 2.8. Проверьте результат

Откройте в браузере: http://89.23.98.91

Если нужно очистить кеш браузера: `Ctrl + Shift + R`

---

## 🔧 Альтернативный способ (через настройку Git на сервере)

### Первоначальная настройка (делается один раз)

```bash
# Подключитесь к серверу
ssh root@89.23.98.91

# Создайте папку для исходников
mkdir -p /var/www/agile-mind-pro-source
cd /var/www/agile-mind-pro-source

# Клонируйте репозиторий
git clone https://github.com/Zorar73/agile-mind-pro.git .

# Установите зависимости
npm install
```

### Последующие обновления (каждый раз)

```bash
# Подключитесь к серверу
ssh root@89.23.98.91

# Перейдите в папку с исходниками
cd /var/www/agile-mind-pro-source

# Получите изменения
git pull origin main

# Установите новые зависимости (если нужно)
npm install

# Соберите проект
npm run build

# Скопируйте файлы
cp -r dist/* /var/www/agile-mind-pro/
chown -R www-data:www-data /var/www/agile-mind-pro/*

# Перезагрузите nginx
nginx -s reload
```

---

## 🤖 Автоматизация деплоя (скрипт)

### Создайте скрипт на сервере

```bash
ssh root@89.23.98.91
nano /root/deploy-agile.sh
```

Вставьте:

```bash
#!/bin/bash

echo "🚀 Начинаем обновление Agile Mind Pro..."

cd /var/www/agile-mind-pro-source

echo "📥 Получаем изменения из GitHub..."
git pull origin main

echo "📦 Устанавливаем зависимости..."
npm install

echo "🔨 Собираем проект..."
npm run build

echo "📂 Копируем файлы..."
cp -r dist/* /var/www/agile-mind-pro/
chown -R www-data:www-data /var/www/agile-mind-pro/*

echo "🔄 Перезагружаем Nginx..."
nginx -s reload

echo "✅ Готово! Проверьте: http://89.23.98.91"
```

Сохраните: `Ctrl + X`, затем `Y`, затем `Enter`

Сделайте исполняемым:

```bash
chmod +x /root/deploy-agile.sh
```

### Использование скрипта

Теперь для деплоя достаточно:

```bash
ssh root@89.23.98.91
/root/deploy-agile.sh
```

---

## ⚠️ Частые проблемы и решения

### Проблема 1: "Permission denied" при git push

**Решение:**
Используйте Personal Access Token от GitHub:
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. Выберите scope: repo
4. Скопируйте токен
5. Используйте токен вместо пароля

### Проблема 2: Изменения не видны на сайте

**Решение:**
1. Очистите кеш браузера: `Ctrl + Shift + R`
2. Проверьте, что файлы скопированы:
   ```bash
   ssh root@89.23.98.91
   ls -la /var/www/agile-mind-pro/
   ```
3. Перезагрузите nginx:
   ```bash
   nginx -s reload
   ```

### Проблема 3: "npm install" падает с ошибкой

**Решение:**
1. Проверьте место на диске:
   ```bash
   df -h
   ```
2. Очистите кеш npm:
   ```bash
   npm cache clean --force
   ```
3. Удалите node_modules и переустановите:
   ```bash
   rm -rf node_modules
   npm install
   ```

### Проблема 4: "Module not found" после деплоя

**Решение:**
Не забудьте установить зависимости:
```bash
npm install
```

### Проблема 5: Сайт не работает после обновления

**Решение:**
1. Проверьте логи Nginx:
   ```bash
   tail -f /var/log/nginx/error.log
   ```
2. Проверьте права доступа:
   ```bash
   chown -R www-data:www-data /var/www/agile-mind-pro/*
   ```
3. Откатитесь к предыдущей версии (см. раздел "Откат")

---

## 🔙 Откат к предыдущей версии

### Если что-то пошло не так

#### Вариант 1: Откат через Git

```bash
ssh root@89.23.98.91
cd /var/www/agile-mind-pro-source

# Посмотрите список коммитов
git log --oneline

# Откатитесь к нужному коммиту
git checkout <commit-hash>

# Пересоберите
npm run build
cp -r dist/* /var/www/agile-mind-pro/
nginx -s reload
```

#### Вариант 2: Бэкап (рекомендуется делать перед каждым деплоем)

```bash
# Перед деплоем
ssh root@89.23.98.91
cd /var/www/agile-mind-pro
tar -czf ../backup-$(date +%Y%m%d-%H%M%S).tar.gz .

# Откат
cd /var/www/agile-mind-pro
rm -rf *
tar -xzf ../backup-YYYYMMDD-HHMMSS.tar.gz
nginx -s reload
```

---

## 📊 Проверка после деплоя

### Чек-лист проверки:

- [ ] Сайт открывается: http://89.23.98.91
- [ ] Логин работает
- [ ] Создание доски работает
- [ ] Система обучения работает
- [ ] Загрузка изображений работает
- [ ] Уведомления приходят
- [ ] Нет ошибок в консоли браузера (F12)

### Мониторинг

Проверьте логи Nginx:
```bash
ssh root@89.23.98.91
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

---

## 🎯 Советы и best practices

### 1. Делайте частые коммиты
Лучше 10 маленьких коммитов, чем один большой.

### 2. Тестируйте локально
Всегда проверяйте изменения локально перед деплоем:
```bash
npm run build
npm run preview
```

### 3. Используйте осмысленные сообщения коммитов
```bash
# Плохо
git commit -m "fix"
git commit -m "update"

# Хорошо
git commit -m "Fix: Исправлена ошибка загрузки обложек курсов"
git commit -m "Feat: Добавлена система попыток экзаменов"
```

### 4. Делайте бэкапы
Перед большими изменениями делайте бэкап:
```bash
ssh root@89.23.98.91
cd /var/www/agile-mind-pro
tar -czf ../backup-$(date +%Y%m%d).tar.gz .
```

### 5. Проверяйте, что пушите
```bash
git status           # Что изменилось
git diff             # Какие изменения
git log --oneline -5 # Последние коммиты
```

---

## 📞 Если что-то пошло не так

1. **Не паникуйте**
2. **Проверьте логи** (Nginx, браузер F12)
3. **Откатитесь** к предыдущей версии
4. **Проверьте** переменные окружения (.env)
5. **Перезагрузите** сервер (в крайнем случае)

### Контакты поддержки Timeweb
- Техподдержка: https://timeweb.cloud/
- Телефон: указан в личном кабинете

---

## 📚 Дополнительные материалы

- [Git основы](https://git-scm.com/book/ru/v2)
- [Nginx документация](https://nginx.org/ru/docs/)
- [Timeweb Cloud гайды](https://timeweb.cloud/help)

---

**Последнее обновление:** 21.12.2024
**Автор:** Agile Mind Pro Team
